<UserControl x:Class="AutoReview.Elements.ManufacturerEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoReview.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="282.608" d:DesignWidth="341.428">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontFamily" Value="Montserrat"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Montserrat"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Montserrat"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#34495E" CornerRadius="10" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Text="Марка производителя:"/>
                <TextBox x:Name="TitleBox" MaxLength="30"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock Text="Страна производителя:"/>
                <TextBox x:Name="CountryBox" MaxLength="50"/>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <TextBlock Text="Директор (email):"/>
                <ComboBox x:Name="DirectorComboBox" 
                          DisplayMemberPath="Email_User"
                          SelectedValuePath="Email_User"
                          Margin="0,0,0,10"/>
            </StackPanel>

            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom">
                <Button Content="Сохранить" 
                        Click="SaveButton_Click"
                        Background="#27AE60"
                        Foreground="White"/>

                <Button Content="Отмена"
                        Click="CancelButton_Click"
                        Background="#E74C3C"
                        Foreground="White"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
